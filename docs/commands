# make a link of the py files to be documented
#otherwise all files, including setup, etc are documented

# the config.py has the path=./, not path=../

# the file source/geneplot_package has been renamed to source/geneplot

sphinx-apidoc -f -o source/ ./
make clean
make html
firefox _build/html/index.html 
